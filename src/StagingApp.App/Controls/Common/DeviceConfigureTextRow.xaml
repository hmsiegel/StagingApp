<UserControl x:Class="StagingApp.Main.Controls.Common.DeviceConfigureTextRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel
        Style="{StaticResource ConfigureStackPanelStyle}">
        <Label
            Style="{StaticResource ConfigureLabelStyle}"
            Content="{Binding ConfigureLabelText}" />
        <TextBox
            Style="{StaticResource ServerConfigureTextBox}"
            Text="{Binding ConfigureTextBox}" />
    </StackPanel>
</UserControl>
